---
title: 權限
description: 了解 hs-sql-agent 2.0.1 管理端採用的「權限路徑 + 動作」授權模型。
sidebar:
  group: 管理
  order: 79
---

import Callout from "@/components/docs/mdx/Callout.astro";
import CardGrid from "@/components/docs/mdx/CardGrid.astro";
import DocCard from "@/components/docs/mdx/DocCard.astro";

<CardGrid columns={3}>
  <DocCard title="權限路徑" icon="PATH" description="識別受保護的管理端資源，例如 /runtime/db-management 或 /auth/user。" />
  <DocCard title="動作" icon="ACT" description="識別可執行的操作，例如 view、create、edit、delete、export。" />
  <DocCard title="角色指派" icon="RBAC" description="角色只能保存伺服器權限範本中存在的合法 permission/action 組合。" />
</CardGrid>

2.0.1 的管理端授權以 **permission path + action code** 成對判斷。控制器宣告每個端點需要的組合，登入成員再透過被指派的角色取得對應權限。

## 實際權限組合

| 受保護操作 | 所需組合 |
| --- | --- |
| 查看 Database Management | `/runtime/db-management` → `view` |
| 建立 Database Management 項目 | `/runtime/db-management` → `create` |
| 編輯語意中繼資料 | `/runtime/db-management/semantic` → `edit` |
| 管理 MCP 金鑰 | `/runtime/mcp-keys` 搭配對應 action |
| 查看使用者 | `/auth/user` → `view` |
| 編輯角色 | `/auth/role` → `edit` |
| 查看稽核紀錄 | `/runtime/audit` → `view` |
| 匯出稽核紀錄 | `/runtime/audit` → `export` |
| 編輯安全政策 | `/runtime/security` → `edit` |
| 重試外送 | `/runtime/operability` → `edit` |

因此，只通過身分驗證仍不足以執行這些操作；所需的 path/action 組合還必須能從該成員被指派的角色中取得。

## `permission-action-templates` 是正式權限清單

Role API 提供 `permission-action-templates`，它是可配置權限的正式清單。每個範本包含：

- permission 的 ID、名稱與 path；
- action 的 ID、code 與名稱。

角色資料以 `{ PermissionId, ActionId }` 組合提交選擇。Role service 會去除重複項目，並且只保存範本清單中真實存在的組合。

<Callout type="warning" title="權限不是可以任意輸入的字串">
不要由用戶端自行發明新的 path/action 字串。角色只能保存伺服器 `permission-action-templates` 已定義的合法組合。
</Callout>

## 角色是權限指派單位

成員會被指派一個或多個角色，而每個角色包含一組 permission/action 選擇：

```text
Member
  └─ Role(s)
       └─ Permission path + Action
```

一位成員可以同時擁有多個角色；實際可用的管理權限，是這些角色權限的聯集。

## 授權變更會讓舊狀態立即失效

編輯角色屬於安全敏感操作。權限改變時，Role service 會找出受影響的成員、遞增其 security version，並透過 auth runtime-state cache barrier 完成狀態更新。

這樣可以避免角色定義已變更，但既有登入工作階段仍長時間沿用舊授權結果。

## 受保護的 `SuperUser` 角色

`SuperUser` 是 2.0.1 內建且受保護的角色，不能透過一般角色管理流程修改或刪除，以免原本具有完整控制權的角色被重新定義成另一套權限集合。

<Callout type="tip" title="依職責設計最小權限角色">
不要替每個管理員帳號都複製 `SuperUser`。例如只負責監看的帳號可能只需要 audit/operability 的 `view`，不需要 database 的 `edit`、key 的 `create` 或 security-policy 的 `edit`。
</Callout>

## 相關文件

- [成員與角色](/zh-hant/docs/administration/members-roles) — 成員生命週期與角色保護機制。
- [Database Management](/zh-hant/docs/administration/database-management) — 資料庫與語意中繼資料的權限範例。
- [MCP Keys](/zh-hant/docs/administration/mcp-keys) — MCP 憑證與管理端角色屬於不同授權邊界。
- [安全政策](/zh-hant/docs/administration/security-policies) — 透過獨立的管理端權限管理 SQL、速率與並行執行政策。
