---
import config from "@/config";
---

<!--
  Astro i18n handles / -> /<defaultLocale>/ because prefixDefaultLocale and
  redirectToDefaultLocale are enabled. This markup is only a static fallback.
-->
<a href={`/${config.site.lang}/`}>Continue to hs-sql-agent</a>
